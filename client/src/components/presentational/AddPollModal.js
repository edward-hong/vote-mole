import React from 'react'
import { Modal } from 'reactstrap'

const AddPollModal = ({ isOpen, toggle }) => (
	<Modal isOpen={isOpen} toggle={toggle}>
		AddPollModal
	</Modal>
)

export default AddPollModal
